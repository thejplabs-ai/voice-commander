# Environment
.env
.env.local

# Python
__pycache__/
*.py[cod]
*.pyd
.venv/
venv/
env/

# Build artifacts — pasta dist (output do PyInstaller) e work temporário
dist/
build/pyinstaller-work/
build/icon_*.png

# Logs
*.log
logs/

# Whisper model cache
~/.cache/huggingface/

# Histórico de transcrições (dado pessoal)
history.jsonl

# Estado local do onboarding (por máquina)
.onboarding_done

# Release zips (artefatos de distribuição — não commitar)
VoiceCommander-*.zip

# Windows
Thumbs.db
desktop.ini
